{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd backend && go build -o keurope ."
  },
  "deploy": {
    "startCommand": "cd backend && ./keurope"
  }
}
