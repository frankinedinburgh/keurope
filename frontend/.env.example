# Admin panel password (set in .env.local, NOT in version control)
VITE_ADMIN_PASSWORD=keurope2024

# API Base URL (optional, defaults to Vercel/Render URLs)
# VITE_API_BASE=http://localhost:5000/api
